import { ElastischInputDirective } from './elastisch-input.directive';

describe('ElastischInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ElastischInputDirective();
    expect(directive).toBeTruthy();
  });
});
